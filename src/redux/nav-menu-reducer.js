
let initialState = {
    links : [
        // {name : "Home", path : "/"},
        // {name : "Items", path : "/items"},
        {name : "Films", path : "/films"},
    ]
}

export const NavMenuReducer = (state = initialState, action) =>{
    switch(action.type){
        default:
            return state
    }
}
